import React from "react"
import Layout from "../components/layout"

const NotFound = ({ data }) => {
    return (
        <Layout>
            <div className="wrap-small">
                <h1>Nothing seems to be here.</h1>
            </div>
        </Layout>
    )
}


export default NotFound
